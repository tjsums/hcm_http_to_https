user  nobody;
worker_processes  1;


events {
    worker_connections  10240;
}

http {
    include       mime.types;

    server {
        server_name     ~^(?<app_id>.+)\.wx\.hcmcloud\.cn$;
        location / {
            rewrite ^ https://$app_id.hcmcloud.cn$request_uri permanent;
        }
    }


    server {
        server_name ~^(?<app_id>.+)\.hcmcloud\.cn$ default;
        location / {
            rewrite ^(.*) https://$server_name$request_uri permanent;
        }

    }
}
